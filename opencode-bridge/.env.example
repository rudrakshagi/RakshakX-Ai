PORT=8787

# Optional. Provider is noAuth by default — only set this if you have
# an OpenCode Zen API key and want to attach it as a Bearer token.
OPENCODE_API_KEY=

# How long the model list is cached before a background refresh, in ms.
MODEL_CACHE_TTL_MS=600000
